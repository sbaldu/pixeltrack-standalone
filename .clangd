CompileFlags:
  Add: [
    -std=c++20,
    -Isrc,
    -Wall,
    -Wextra,
    -Wpedantic,
    -ferror-limit=0
  ]
  Remove: [-m*, -O*, -g*]
  FallbackFlags: [-std=c++20, -Iinclude]
  CompilationDatabase: build
